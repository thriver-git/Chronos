services:
  engine:
    build: .
    env_file: .env
    volumes:
      - ./logs:/app/logs
  dashboard:
    build: .
    command: streamlit run dashboard/app.py --server.address 0.0.0.0
    env_file: .env
    ports:
      - "8501:8501"
    volumes:
      - ./logs:/app/logs
